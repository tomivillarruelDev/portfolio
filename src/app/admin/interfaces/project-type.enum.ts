/**
 * Enumeración que define los tipos de proyectos en el portfolio
 * - CARD: Representa proyectos mostrados en formato de tarjeta
 * - IMAGE: Representa proyectos con imagen destacada
 */
export enum ProjectType {
  CARD = 'projects',
  IMAGE = 'projects-image',
}
