export interface ExperienceInterface {
  name: string;
  company: string;
  date: string;
  address: string;
  description?: string;
  tasks?: string[];
  stack?: string;
  metric?: string;
  metricLabel?: string;
}
